<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sowhat.shakes.admin.user.mapper.UserMapper">

    <insert id="insertUser" parameterType="com.sowhat.shakes.base.mode.UserInfo" useGeneratedKeys="true" keyColumn="id">
        INSERT INTO user_info(id,name,password,mobile) VALUES (#{id},#{name},#{password},#{mobile});
    </insert>

    <update id="updateUser" parameterType="com.sowhat.shakes.base.mode.UserInfo">
        UPDATE user_info set name=#{name},password=#{password},mobile=#{mobile} where id=#{id};
    </update>

    <delete id="deleteUser" parameterType="String">
        delete from user_info where id=#{id};
    </delete>

    <select id="findById" parameterType="String" resultType="com.sowhat.shakes.base.mode.UserInfo">
        select * from user_info where id=#{userInfo.id};
    </select>

    <select id="find" parameterType="com.sowhat.shakes.base.mode.UserInfo" resultType="java.util.List">
        SELECT * from user_info ;
    </select>

</mapper>
